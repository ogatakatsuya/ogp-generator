{
  "name": "ogp-generator",
  "version": "1.0.0",
  "description": "This is ogp generator function deployed to AWS Lambda.",
  "main": "app.ts",
  "scripts": {
    "build": "esbuild app.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=app.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@aws-lambda-powertools/logger": "^2.22.0",
    "@types/aws-lambda": "^8.10.150",
    "@types/node": "^24.0.4",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  }
}
